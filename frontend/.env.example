# Point the frontend at the backend in local dev (optional).
# Must include /api so paths like /auth/signup become http://localhost:3001/api/auth/signup
# If not set, requests go to /api and rely on Vite's proxy to http://localhost:3001
VITE_API_URL=http://localhost:3001/api
