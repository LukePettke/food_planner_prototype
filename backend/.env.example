# Server
PORT=3001

# Auth (required for sign-in; use a long random string in production)
JWT_SECRET=your-jwt-secret-change-in-production

# OpenAI (required for AI meal suggestions; also used for recipe + shopping list when Spoonacular misses)
# Without this, the app uses mock data for suggestions and AI fallback for recipes.
OPENAI_API_KEY=your-openai-api-key

# Spoonacular (optional - real recipes tried first; fallback to AI or mock if missing)
# Get key at https://spoonacular.com/food-api/console (free tier available)
SPOONACULAR_API_KEY=your-spoonacular-api-key

# Unsplash (optional - for meal photos; 50 requests/hour free tier)
# Get key at https://unsplash.com/developers
UNSPLASH_ACCESS_KEY=your-unsplash-access-key

# Pexels (optional - meal photos; 200 requests/hour free)
# Get key at https://www.pexels.com/api/
PEXELS_API_KEY=your-pexels-api-key

# Add PEXELS_API_KEY above for meal photos.

# Google Calendar (optional - for adding meal plan events)
# Create OAuth 2.0 credentials at https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:5173/integrations